import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors (Professional Blue)
  static const Color primary = Color(0xFF2196F3);
  static const Color primaryDark = Color(0xFF1976D2);
  static const Color primaryLight = Color(0xFFBBDEFB);

  // Accent Colors (Complementary Orange)
  static const Color accent = Color(0xFFFF9800);
  static const Color accentDark = Color(0xFFF57C00);

  // Neutral Colors (for Typography & Backgrounds)
  static const Color backgroundLight = Color(0xFFF5F7FA); // Soft gray-blue
  static const Color backgroundDark = Color(0xFF121212);  // AMOLED black
  static const Color surfaceLight = Colors.white;
  static const Color surfaceDark = Color(0xFF1E1E1E);

  // Semantic Colors
  static const Color error = Color(0xFFE53935);
  static const Color success = Color(0xFF43A047);
  static const Color warning = Color(0xFFFB8C00);
}